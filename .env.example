# URL of the Python backend (FastAPI). In development this is typically http://localhost:8000.
# In production, set this to your deployed Cloud Run backend URL.
VITE_API_URL="https://truckdriverapp-production.up.railway.app"
